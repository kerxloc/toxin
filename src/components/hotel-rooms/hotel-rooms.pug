include ../typography/typography
include ../hotel-card/hotel-card
include ../pagination/pagination

mixin hotelRooms(hotels)
  section.hotels
    .hotels__wrap
      .hotels__title-wrap
        +typography({ variant: "h1", text: "Номера, которые мы для вас подобрали" })
      ul.hotels__list
        each item in hotels
          li.hotels__item
            +hotelCard(item)
      .hotels__pagination      
        +pagination()